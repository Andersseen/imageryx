# Dependencies
node_modules
.pnpm-store

# Env
.env
.env.local
.env.*.local
!.env.example

# Build outputs
dist
out
build
.output
.vercel
.analog
.angular
*.tsbuildinfo

# Turborepo
.turbo

# Wrangler / Cloudflare
.wrangler
.wrangler-state
# Disposable E2E state — namespaced away from .wrangler-state so an E2E run can never
# upload into or delete from the database `pnpm dev` is using.
.wrangler-state-e2e
.dev.vars
worker-configuration.d.ts

# Local provider storage (LocalStorageProvider's root — see packages/providers)
.local/

# Testing
coverage
playwright-report
test-results

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json
.idea
