{
  "name": "@imageryx/image-core",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "description": "Provider-independent image domain services: filename/path normalization, MIME validation, checksums, preset normalization and hashing, transformation validation, and provider selection.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@imageryx/contracts": "workspace:*"
  },
  "devDependencies": {
    "@imageryx/eslint-config": "workspace:*",
    "@imageryx/typescript-config": "workspace:*",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
